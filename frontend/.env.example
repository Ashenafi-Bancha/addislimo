# Copy to `.env.local` (git-ignored) and fill in.
# Only VITE_-prefixed variables are exposed to the browser bundle.

# Base URL of the Addis Limo API. Leave empty until the Express backend runs —
# while empty, src/lib/api/client.ts throws instead of firing doomed requests.
VITE_API_URL=
