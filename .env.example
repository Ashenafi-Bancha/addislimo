# Workspace-level example env. Each app also has its own:
#   frontend/.env.example  — browser-safe VITE_* variables
#   backend/.env.example   — added when the API is scaffolded
#
# Planned backend variables (documented here so nothing is a surprise later):
# PORT=4000
# NODE_ENV=development
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/addislimo
# JWT_SECRET=
# CORS_ORIGIN=http://localhost:8443
